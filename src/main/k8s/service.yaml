apiVersion: v1
kind: Service
metadata:
  name: api-traces-quarkus
  namespace: default
  labels:
    app: api-traces-quarkus
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: api-traces-quarkus
  type: NodePort
